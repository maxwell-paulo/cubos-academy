const pi = Math.PI;
let raio = 1;
const comprimento = 2 * pi * raio;
const area = pi * raio ** 2;

console.log(
  `Para um circulo de raio ${raio}, temos comprimento de ${comprimento.toFixed(
    2
  )} e área de ${area.toFixed(2)}`
);
